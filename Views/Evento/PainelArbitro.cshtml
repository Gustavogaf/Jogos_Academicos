@model IEnumerable<Jogos_Academicos.Models.Evento>

@{
    ViewData["Title"] = "Gestão de Torneio";
}

<link rel="stylesheet" href="~/css/gestao-torneio.css" />

<div class="container-torneio">
    <h2 class="torneio-title">Painel de Gestão do Árbitro</h2>

    @if (TempData["Sucesso"] != null)
    {
        <div class="torneio-alert-success">@TempData["Sucesso"]</div>
    }
    @if (TempData["Erro"] != null)
    {
        <div class="torneio-alert-error">@TempData["Erro"]</div>
    }

    <div class="torneio-card">
        <div class="torneio-card-header">
            Eventos Disponíveis
        </div>
        <div class="torneio-card-body">
            @if (!Model.Any())
            {
                <p>Não há eventos cadastrados.</p>
            }
            else
            {
                <table class="torneio-table">
                    <thead>
                        <tr>
                            <th>Evento</th>
                            <th>Status</th>
                            <th>Ações de Controle</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            bool temJogos = item.Grupos != null && item.Grupos.SelectMany(g => g.Jogos).Any();

                            <tr>
                                <td>
                                    <div class="evento-nome">@item.Nome</div>
                                    <div class="evento-detalhe">
                                        @item.Esporte?.Nome - @item.Data.ToString("dd/MM/yyyy")
                                    </div>
                                </td>
                                <td>
                                    @if (!temJogos)
                                    {
                                        <span class="status-badge status-nao-iniciado">Não Iniciado</span>
                                    }
                                    else
                                    {
                                        <span class="status-badge status-andamento">Em Andamento</span>
                                    }
                                </td>
                                <td>
                                    <div class="acoes-container">
                                        @if (item.Grupos != null && item.Grupos.Any())
                                        {
                                            <a asp-controller="Grupo"
                                               asp-action="Detalhes"
                                               asp-route-id="@item.Grupos.First().Id"
                                               class="btn-acao btn-ver">
                                                👁 Ver Tabelas
                                            </a>
                                        }

                                        @if (!temJogos)
                                        {
                                            <form asp-action="GerarJogosFaseGrupos" method="post">
                                                <input type="hidden" name="eventoId" value="@item.Id" />
                                                <button type="submit" class="btn-acao btn-gerar">
                                                    🎲 Gerar Fase de Grupos
                                                </button>
                                            </form>
                                        }
                                        else
                                        {
                                            <form asp-action="AvancarFase" method="post"
                                                  onsubmit="return confirm('Deseja avançar para a próxima fase?');">
                                                <input type="hidden" name="eventoId" value="@item.Id" />
                                                <button type="submit" class="btn-acao btn-avancar">
                                                    ⚡ Gerar Próxima Fase
                                                </button>
                                            </form>
                                        }
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
        </div>
    </div>

    <div class="torneio-info">
        O botão <strong>Gerar Fase de Grupos</strong> cria a tabela inicial de pontos corridos.<br />
        O botão <strong>Gerar Próxima Fase</strong> cria as chaves eliminatórias.
    </div>
</div>